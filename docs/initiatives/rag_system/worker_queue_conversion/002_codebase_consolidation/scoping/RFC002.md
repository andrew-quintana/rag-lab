# RFC 002 — Codebase Consolidation & Production Readiness

## Context

This RFC defines the technical approach for consolidating the codebase structure, eliminating duplication, and ensuring production readiness for the worker-queue architecture implemented in Initiative 001.

**Status**: The current Azure Functions deployment includes duplicate backend code and fragmented configuration. This RFC provides the technical design for consolidation and production readiness validation.

## Design Overview

The consolidation approach focuses on three main areas:
1. **Code Duplication Elimination**: Remove duplicate code and ensure functions import from source
2. **Configuration Consolidation**: Unify environment variable and configuration management
3. **Test Infrastructure Consolidation**: Standardize test execution and utilities

### High-Level Consolidation Strategy

```
Current State:
┌─────────────────────────────────────────────────────────┐
│  backend/src/ (source)                             │
│  └── [all backend code]                                 │
└─────────────────────────────────────────────────────────┘
           │
           │ (copied during build)
           ▼
┌─────────────────────────────────────────────────────────┐
│  infra/azure/azure_functions/backend/src/ (copy)   │
│  └── [duplicate backend code]                           │
└─────────────────────────────────────────────────────────┘

Target State:
┌─────────────────────────────────────────────────────────┐
│  backend/                                              │
│  ├── src/ (source - single source of truth)      │
│  │   └── [all backend code]                            │
│  └── azure_functions/ (moved here)                      │
│      ├── ingestion-worker/                             │
│      │   └── __init__.py                               │
│      │       └── from src.services.workers...     │
│      ├── chunking-worker/                              │
│      ├── embedding-worker/                             │
│      └── indexing-worker/                              │
└─────────────────────────────────────────────────────────┘
           │
           │ (simple direct imports - no path manipulation)
           │
           All backend code in one place!
```

## Technical Design

### 1. Code Duplication Elimination

#### 1.1 Move Azure Functions to Backend
- Move `infra/azure/azure_functions/` directory to `backend/azure_functions/`
- Update all references to the new location
- Update deployment scripts and configuration

#### 1.2 Simplify Function Entry Points
- Update all 4 function `__init__.py` files to use simple direct imports
- No path manipulation needed - functions are now in `backend/` alongside `src/`
- Remove dotenv loading (Azure handles environment variables automatically)

**Example Pattern:**
```python
"""Azure Function for ingestion worker"""
import json
from src.services.workers.ingestion_worker import ingestion_worker
from src.core.logging import get_logger

logger = get_logger("azure_functions.ingestion_worker")

def main(queueMessage: str) -> None:
    """Azure Function entry point"""
    try:
        message_dict = json.loads(queueMessage)
        logger.info(f"Processing ingestion message for document: {message_dict.get('document_id')}")
        ingestion_worker(message_dict, context=None)
        logger.info(f"Successfully processed ingestion for document: {message_dict.get('document_id')}")
    except Exception as e:
        logger.error(f"Error processing ingestion message: {e}", exc_info=True)
        raise
```

#### 1.3 Simplify Build Script
- Remove code copying logic from `build.sh`
- Build script should only verify prerequisites and prepare deployment package
- No need to copy backend code since functions import directly from `src`

### 2. Configuration Consolidation

#### 2.1 Environment Variable Loading Strategy
- **Azure Functions (Cloud)**: Environment variables from Azure Function App settings (automatically available via `os.environ`)
- **Local Development**: Optional `.env.local` loaded via dotenv in function entry points (if file exists)
- **Unit Tests**: Environment variables set via pytest fixtures or `os.environ` (no file dependency)
- **Precedence**: Azure Function App settings > `.env.local` (when loaded) > system environment > test fixtures
- **Implementation**: Function entry points check for `.env.local` and load it if it exists, but don't require it. Azure settings and test fixtures take precedence.

#### 2.2 Configuration Files
- **`local.settings.json`**: Only Azurite connection string and runtime settings (`AzureWebJobsStorage`, `AZURE_STORAGE_QUEUES_CONNECTION_STRING`, `FUNCTIONS_WORKER_RUNTIME`)
- **`.env.local`** (Optional): Application configuration for local development (database, Azure AI services, etc.). Not required - functions can use system environment variables.
- **Azure Function App Settings**: Production configuration (secrets, endpoints, etc.) - automatically available via `os.environ`
- **Test Configuration**: Environment variables set via pytest fixtures or `os.environ` in test setup (no file dependency)

#### 2.3 Documentation
- Document configuration precedence clearly
- Provide examples for local and cloud configuration
- Document migration path from current to consolidated structure

### 3. Test Infrastructure Consolidation

#### 3.1 Test Execution Unification
- Create unified test execution scripts for local and cloud
- Use pytest markers (`@pytest.mark.local`, `@pytest.mark.cloud`) consistently
- Register markers in `pytest.ini` or `conftest.py` to eliminate warnings

#### 3.2 Test Fixtures Consolidation
- Consolidate duplicate test fixtures
- Create shared fixtures for common test setup (database, queues, etc.)
- Standardize test utilities and helpers

#### 3.3 Test Scripts
- `scripts/test_functions_local.sh`: Run local Phase 5 tests
- `scripts/test_functions_cloud.sh`: Run cloud Phase 5 tests
- `scripts/test_functions_all.sh`: Run all tests with appropriate markers

### 4. Deployment Process Optimization

#### 4.1 Build Script Simplification
- Remove backend code copying
- Verify function entry points are correct
- Validate requirements.txt is present
- Prepare deployment package without code duplication

#### 4.2 Deployment Verification
- Verify functions can import from project root in Azure environment
- Test that path resolution works correctly in cloud
- Validate environment variable loading in production

## Implementation Phases

### Phase 1: Code Duplication Elimination & Structure Reorganization
1. Move Azure Functions from `infra/azure/azure_functions/` to `backend/azure_functions/`
2. Simplify function entry points to use direct imports (no path manipulation)
3. Update build script and deployment configuration
4. Test local function execution with new structure

### Phase 2: Configuration Consolidation
1. Document configuration loading strategy
2. Standardize `.env.local` and `local.settings.json` usage
3. Update documentation with configuration precedence
4. Test configuration loading in local and cloud environments

### Phase 3: Test Infrastructure Consolidation
1. Register pytest markers
2. Consolidate test fixtures and utilities
3. Create unified test execution scripts
4. Run Phase 5 tests with consolidated infrastructure

### Phase 4: Production Readiness Validation
1. Complete Phase 5 local testing
2. Complete Phase 5 cloud testing
3. Resolve all identified issues
4. Validate performance requirements
5. Verify production deployment stability

## Risks & Mitigations

### Risk 1: Import Path Issues After Move
**Mitigation**: Test imports thoroughly after moving functions to `backend/`. Since both `src` and `azure_functions` are in `backend/`, imports should work naturally. Have rollback plan if needed.

### Risk 2: Import Errors in Production
**Mitigation**: Comprehensive testing in staging environment. Monitor function logs closely after deployment.

### Risk 3: Configuration Precedence Confusion
**Mitigation**: Clear documentation and examples. Validation scripts to check configuration.

### Risk 4: Test Infrastructure Breaking Changes
**Mitigation**: Incremental consolidation with testing at each step. Maintain backward compatibility where possible.

## Success Metrics

- Deployment package size reduction (target: 50%+ reduction)
- Build time reduction (target: 30%+ reduction)
- Code duplication eliminated (target: 0 duplicate files)
- All Phase 5 tests passing (target: 100% pass rate)
- Configuration clarity (target: documented and validated)

---

**Last Updated**: 2025-12-07  
**Related Initiative**: 001 (Worker-Queue Architecture Implementation)  
**Status**: Ready for implementation

